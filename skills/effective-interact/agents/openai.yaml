interface:
  display_name: "Effective Interact"
  short_description: "复杂沟通的结构化交付选择"
  default_prompt: "Use $effective-interact to structure a complex decision, plan, review, or handoff."

policy:
  allow_implicit_invocation: true
